// src/interfaces.ts
interface Article {
    id: number;
    title: string;
    content: string;
    createdAt: string; // Змінили
    author: string;   // Змінили
    category: string;  // Змінили
}

export default Article;
